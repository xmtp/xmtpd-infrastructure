apiVersion: v1
kind: Service
metadata:
  name: {{ include "xmtp-gateway.fullname" . }}
  labels:
    {{- include "xmtp-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "xmtp-gateway.selectorLabels" . | nindent 4 }}
